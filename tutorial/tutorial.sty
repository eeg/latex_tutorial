\ProvidesPackage{tutorial}
% This line is here only because the preamble is in this separate file.  The argument above must match the name of this file.

%--------------------------------------------------
% Paper size and margins
%--------------------------------------------------

\usepackage[paper=letterpaper, margin=1in]{geometry}

%--------------------------------------------------
% Spacing
%--------------------------------------------------

% Paragraphs
\parindent 0pt
\parskip 10pt

% For text that is not right-justified, uncomment this
% \raggedright

% Double-spacing
\usepackage{setspace}
\setstretch{1.0}
% Put whatever number you want here.  1.0 is single-spaced and 2.0 is double-spaced.

%--------------------------------------------------
% Custom commands
%--------------------------------------------------

% For printing LaTeX code without executing it.
\usepackage{listings}
\newcommand{\latexcode}[1]{\lstinline[language={[LaTeX]TeX},basicstyle=\ttfamily]{#1}}

% For naming each subsection that is a task or exercise.
\newcommand{\task}{Try this}

%--------------------------------------------------
% Section headers
%--------------------------------------------------

\usepackage[compact,tiny]{titlesec}

\titleformat*{\section}{\bfseries}
\titleformat*{\subsection}{\itshape\bfseries}
\titleformat*{\subsubsection}{\itshape} % for italic
% \titleformat*{\subsubsection}{\scshape} % for small caps

%--------------------------------------------------
% Within-text references
%--------------------------------------------------

\usepackage{nameref}

\usepackage[capitalize,sort&compress]{cleveref}
\crefname{figure}{Fig.}{Figs.}
% \crefformat{figure}{figure~#2#1#3}
\crefname{table}{Table}{Tables}
\crefname{section}{Sect.}{Sects.}
\crefname{equation}{Eq.}{Eqs.}
% \crefformat{equation}{Eq.~#2#1#3}
% \crefrangeformat{equation}{Eqs.~#3#1#4--#5#2#6}

%--------------------------------------------------
% Bibliography
%--------------------------------------------------

\usepackage{natbib}
% \usepackage[numbers, sort&compress]{natbib}

% Picky matters can be fixed up.
% Many of our journals specify no comma between the author and year:
\bibpunct{(}{)}{;}{a}{}{,}
% Don't waste extra space between entries:
\setlength{\bibsep}{0pt}

%--------------------------------------------------
% Figures
%--------------------------------------------------

\usepackage{graphicx} % to include graphics files

%--------------------------------------------------
% Tables
%--------------------------------------------------

\usepackage{booktabs}
\usepackage{multirow}
% \renewcommand\multirowsetup{\centering}
\usepackage{tabularx}
\heavyrulewidth 0.09em
\cmidrulekern 0.2em

%--------------------------------------------------
% Math
%--------------------------------------------------

\usepackage{amsmath} % for fancier-than-default math typesetting

%--------------------------------------------------
% Notes-to-self
%--------------------------------------------------

% Here is a way to put comments into the compiled document.  (Rather than only having them in the source document, like all this text after the % sign.)
\usepackage[colorinlistoftodos]{todonotes}

%--------------------------------------------------
% Header and Footer
%--------------------------------------------------

\usepackage{fancyhdr}

%--------------------------------------------------
% Line numbering
%--------------------------------------------------

\usepackage{lineno}
\modulolinenumbers[5] % which lines to number
% \linenumbers % turn on the numbering
